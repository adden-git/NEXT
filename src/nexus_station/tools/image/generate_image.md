Generate an image using AI (Fireworks FLUX.1 Dev).

- Provide a detailed text `prompt` describing what you want to see.
- Optionally provide an `init_image` (base64 data URL or URL) to use as a reference for img2img generation.
- Returns a markdown image that will be displayed in the chat.
